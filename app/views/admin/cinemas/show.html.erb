<%= provide(:title, "#{@cinema.name}")%>

<%= link_to @cinema.name, edit_admin_cinema_path(@cinema) %>

<%= link_to "Delete", admin_cinema_path(@cinema), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>